{% extends 'backoffice/layout.html.twig' %}
{% block suite_body %}
    <h1>Ajout d'un professeur</h1>
    {{form_start(formTeacher)}}
    {{form_errors(formTeacher)}}
    {{form_widget(formTeacher)}}
    <button>Enregistrer le professeur</button>
    {{form_end(formTeacher)}}
{% endblock %}